# CLAUDE.md — SiteGuard

Module bảo mật dùng chung (`@suga/site-guard`) — chống crawl/bot, rate-limit, tăng tốc; là **thư viện** repo khác `import`, KHÔNG chạy riêng. Repo `Debill84/site-guard`, publish theo tag để ghim. Test: `npm test`.
⚠️ Riêng: đổi API phải versioning + ghim tag (nhiều nơi dùng) · khi cắm vào app phải LOẠI TRỪ endpoint thanh toán/cron/webhook.

🧠 **Bộ nhớ:** kho này **CÔNG KHAI** ⇒ sổ tay **không được nêu tên tủ/ngăn/mảnh**, kể cả đường dẫn
tương đối (luật 12/08/2026 — dòng cũ có nêu, đã gỡ). Đường tra nằm ở sổ tay gốc của máy, mở phiên
là tự nạp. **Đừng chép lại vào đây.**
